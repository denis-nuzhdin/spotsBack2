spring:
  datasource:
    url: jdbc:postgresql://db:5432/sportsocial_test
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    com.sportsocial: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG 