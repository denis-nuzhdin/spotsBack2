spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/sportsocial_test
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    default-property-inclusion: ALWAYS

logging:
  level:
    com.sportsocial: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG 